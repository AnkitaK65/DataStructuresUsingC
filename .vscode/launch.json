{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",                     // ✅ use CodeLLDB
            "request": "launch",
            "name": "Debug with LLDB",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "Build current file" // keep build step
        }
    ]
}